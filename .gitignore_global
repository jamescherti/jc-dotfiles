# General
ctags
tags

# Editors
*~
*.swp
*.swo
*#

# Ansible
*.log
*.retry

# Emacs
flymake_*
*.elc
.cask/
.eask/

# Emacs: Flycheck
# To safely ignore Flycheck temporary files without overmatching, consider
# setting `flycheck-temp-prefix` to "flycheck_" in your Emacs config:
#   (setq flycheck-temp-prefix "flycheck_")
# This ensures the pattern `flycheck_*` matches only Flycheck temp files.
flycheck_*

# Python
__pycache__/
*.pyc
*.pyo
.coverage

# Ruby
*.rbc

# Compilers
*.class
*.o
*.a
*.so

# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini
ehthumbs.db
Thumbs.db

# VCS
.svn/
